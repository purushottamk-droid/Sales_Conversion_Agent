# ── Google ADK ──────────────────────────────────────
google-adk

# ── BigQuery — Agent 1 ──────────────────────────────
google-cloud-bigquery

# ── Vertex AI — Agent 2 Gemini calls ────────────────
google-cloud-aiplatform

# ── Pydantic — Agent 2 output schema ────────────────
pydantic

# ── Environment variables ────────────────────────────
python-dotenv

mcp==1.28.1  # pinned — 2.0.0 renamed CallToolResult.isError to is_error,
             # breaking _call_mcp_tool in data_collection_custom_agent/agent.py
             # and decision_action_agent/tools.py (confirmed via production
             # Cloud Run logs: AttributeError on every Salesforce MCP call)

google-api-python-client


sqlalchemy
aiosqlite
asyncpg